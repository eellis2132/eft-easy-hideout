<Window x:Class="EasyHideout.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource BgSurfaceBrush}">
    <StackPanel Margin="24">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13"
                   Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,24"
                   LineHeight="20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelBtn" Content="Cancel" Width="90" Margin="0,0,8,0"
                    Click="CancelBtn_Click"/>
            <Button x:Name="ConfirmBtn" Content="Confirm" Width="90"
                    Style="{DynamicResource DangerButton}"
                    Click="ConfirmBtn_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
